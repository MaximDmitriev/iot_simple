import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
  paper: {
    width: '650px',
    margin: 'auto',
    marginTop: '100px',
    padding: '30px',
  },
}));
